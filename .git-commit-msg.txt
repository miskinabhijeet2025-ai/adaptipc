Add TikZ diagrams, data-driven charts, and evaluation scoping fixes

- Fig: system architecture data flow (TikZ, Section IV)
- Fig: per-message routing decision flowchart incl. escalation guard
  (Section III-C)
- Fig: adapt_recv() multiplexer loop (Section IV-D)
- Fig: alpha sensitivity tradeoff, generated from
  benchmarks/alpha_sensitivity.txt by scripts/plot_alpha_sensitivity.py
- Fig: latency decomposition, generated from
  benchmarks/latency_breakdown_*.txt by scripts/plot_latency_breakdown.py
- Fix unbalanced math delimiter in Section III-D; escape AF_UNIX
  underscore in figure caption
- Scope throughput claims to the evaluation baselines; add Related Work
  on iceoryx, ZeroMQ, NNG, and io_uring
- Reframe Introduction motivation as per-channel vs per-message selection;
  add lazy endpoint negotiation to Future Work with measured adapt_init()
  cost (benchmarks/init_latency.txt)
- Sanitize absolute paths from preserved run logs
